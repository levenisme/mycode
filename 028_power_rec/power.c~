unsigned power(unsigned x, unsigned y) {
  unsigned ans = 1;
  if (x == 0 && y == 0) {
    ans = 1;
  }
  else if (y == 0) {
    ans = 1;
  }
  else {
    for (unsigned i = 0; i < y; i++) {
      ans = ans * x;
    }
  }
  return ans;
}
